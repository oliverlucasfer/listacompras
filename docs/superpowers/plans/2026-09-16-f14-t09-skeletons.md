# F14-T09 — Skeletons + fechamento — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Introduzir um placeholder de carregamento estático (`AppEsqueleto`, sem animação e sem pacote novo) nas três listas com maior chance de "piscar" — painel, itens da lista e membros — e fechar a Fase 14.

**Architecture:** Um componente novo no design system (`lib/core/widgets/app_esqueleto.dart`) e três trocas de estado de carregamento (`Center(CircularProgressIndicator())` → `AppEsqueleto`). Sem schema/RLS/IA/sync. O spinner continua permitido onde o conteúdo é pequeno (ações de botão).

**Tech Stack:** Flutter 3.44 · flutter_riverpod 3 · go_router 18 · Drift · flutter_test.

**Spec:** `docs/superpowers/specs/2026-09-14-ux-acessibilidade-design.md` (§11)

**Tarefa dona:** F14-T09 em `docs/14-tarefas.md` · Docs normativos: [15 §3](../../15-design-system.md), [05 §7](../../05-app-flutter.md)

## Global Constraints

- Comentários no código apenas quando indispensáveis. `camelCase` (Dart); pt-BR strings centralizadas em `lib/core/l10n/app_strings.dart`.
- Testes: nome `deve_<resultado>_quando_<condicao>`.
- **Não alterar schema/RLS/IA/sync nem repositórios.** Sem dependência/pacote novo; **sem animação** (placeholder estático).
- Componente novo em `lib/core/widgets/`; a cor é do tema (`colorScheme.surfaceContainerHighest`); medidas em tokens (`AppSpacing`/`AppRadius`).
- Rodar antes de fechar: `dart format .`, `flutter analyze` e `flutter test` verdes.
- A distribuição de APK aos testadores via F5-T05b é **opcional** — não executar sem solicitação explícita.
- **Não commitar** sem confirmação explícita do usuário (a validação fecha a tarefa).

---

### Task 1: Componente `AppEsqueleto`

**Files:**
- Create: `lib/core/widgets/app_esqueleto.dart`
- Test: `test/core/widgets/app_esqueleto_test.dart`

**Interfaces:**
- Produces: `AppEsqueleto({int linhas = 4, double altura = 56})` — `StatelessWidget` que renderiza `linhas` blocos retangulares full-width (cantos `AppRadius.md`, cor `colorScheme.surfaceContainerHighest`, separados por `AppSpacing.sm`, com padding `AppSpacing.lg` horizontal / `AppSpacing.sm` vertical), envolvido em `Semantics(label: AppStrings.carregando, container: true)`. Consumido na Task 2 e no catálogo (Task 3).

- [ ] **Step 1: Escrever o teste que falha** — `test/core/widgets/app_esqueleto_test.dart`

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:lista_compras/core/l10n/app_strings.dart';
import 'package:lista_compras/core/theme/app_theme.dart';
import 'package:lista_compras/core/widgets/app_esqueleto.dart';

void main() {
  testWidgets('deve_exibir_linhas_estaticas_quando_renderizado', (
    tester,
  ) async {
    final handle = tester.ensureSemantics();
    await tester.pumpWidget(
      MaterialApp(
        theme: AppTheme.claro,
        home: const Scaffold(body: AppEsqueleto(linhas: 3)),
      ),
    );

    expect(
      find.descendant(
        of: find.byType(AppEsqueleto),
        matching: find.byType(Container),
      ),
      findsNWidgets(3),
    );
    // Placeholder estático: sem spinner.
    expect(find.byType(CircularProgressIndicator), findsNothing);
    // Anuncia o carregamento para o leitor de tela.
    expect(find.bySemanticsLabel(AppStrings.carregando), findsOneWidget);

    handle.dispose();
  });
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `flutter test test/core/widgets/app_esqueleto_test.dart`
Expected: FAIL (`app_esqueleto.dart` não existe).

- [ ] **Step 3: Implementar** — `lib/core/widgets/app_esqueleto.dart`

```dart
import 'package:flutter/material.dart';

import '../l10n/app_strings.dart';
import '../theme/tokens/app_radius.dart';
import '../theme/tokens/app_spacing.dart';

/// Placeholder estático de carregamento das listas (doc 15 §3, F14-T09):
/// blocos com a cor de superfície do tema, sem animação e sem pacote novo.
class AppEsqueleto extends StatelessWidget {
  const AppEsqueleto({super.key, this.linhas = 4, this.altura = 56});

  final int linhas;
  final double altura;

  @override
  Widget build(BuildContext context) {
    final cor = Theme.of(context).colorScheme.surfaceContainerHighest;
    return Semantics(
      label: AppStrings.carregando,
      container: true,
      child: Padding(
        padding: const EdgeInsets.symmetric(
          horizontal: AppSpacing.lg,
          vertical: AppSpacing.sm,
        ),
        child: Column(
          children: [
            for (var i = 0; i < linhas; i++) ...[
              if (i > 0) const SizedBox(height: AppSpacing.sm),
              Container(
                width: double.infinity,
                height: altura,
                decoration: BoxDecoration(
                  color: cor,
                  borderRadius: AppRadius.mdTodos,
                ),
              ),
            ],
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `flutter test test/core/widgets/app_esqueleto_test.dart`
Expected: PASS.

---

### Task 2: Usar `AppEsqueleto` no painel, itens e membros

**Files:**
- Modify: `lib/features/listas/ui/painel_listas.dart` (loading do `listasAsync.when`)
- Modify: `lib/features/listas/ui/tela_lista_screen.dart` (loading de `_ListaItens`)
- Modify: `lib/features/convites/ui/tela_membros_screen.dart` (loading de `membrosAsync.when`)
- Test: `test/features/listas/tela_lista_screen_test.dart`

**Interfaces:**
- Consumes: `AppEsqueleto` (Task 1).

- [ ] **Step 1: Escrever o teste que falha** — em `test/features/listas/tela_lista_screen_test.dart` (perto dos testes de estado de erro/não encontrada)

```dart
  testWidgets('deve_mostrar_esqueleto_quando_itens_carregando', (tester) async {
    final repo = ListasRepository(db);
    final lista = await repo.criarLista(titulo: 'Compras', donoId: 'user-a');
    final itens = StreamController<List<Item>>();
    addTearDown(itens.close);
    final sync = StreamController<SyncStatus>();
    sync.add(const Sincronizado());
    addTearDown(sync.close);

    await tester.pumpWidget(
      ProviderScope(
        overrides: [
          appDatabaseProvider.overrideWithValue(db),
          itensDaListaProvider(lista.id).overrideWith((ref) => itens.stream),
          papelRepositoryProvider.overrideWithValue(
            papelRepo(tester, listaId: lista.id, papel: Papel.dono),
          ),
          syncStatusProvider.overrideWith((ref) => sync.stream),
        ],
        child: MaterialApp(home: TelaListaScreen(listaId: lista.id)),
      ),
    );
    await tester.pumpAndSettle();

    expect(find.byType(AppEsqueleto), findsOneWidget);

    await fechar(tester);
  });
```

Adicionar o import `package:lista_compras/core/widgets/app_esqueleto.dart` no topo do teste (`dart:async`, `Item`, `SyncStatus`/`Sincronizado`, `StreamController` já estão importados no arquivo).

- [ ] **Step 2: Rodar e ver falhar**

Run: `flutter test test/features/listas/tela_lista_screen_test.dart --plain-name deve_mostrar_esqueleto_quando_itens_carregando`
Expected: FAIL (`AppEsqueleto` ainda não é renderizado).

- [ ] **Step 3: Implementar as três trocas** (adicionar o import `app_esqueleto.dart` em cada arquivo)

`painel_listas.dart` — no `body: listasAsync.when(...)`:

```dart
        loading: () => const AppEsqueleto(linhas: 4),
```

`tela_lista_screen.dart` — no `_ListaItens`, em `itensAsync.when(...)`:

```dart
      loading: () => const AppEsqueleto(linhas: 5),
```

`tela_membros_screen.dart` — no `body: membrosAsync.when(...)`:

```dart
          loading: () => const AppEsqueleto(linhas: 4),
```

**(Não** trocar o loading do `Scaffold` externo da tela da lista — só o da lista de itens.)

- [ ] **Step 4: Rodar e ver passar**

Run: `flutter analyze` e `flutter test test/features/listas/ test/features/convites/`
Expected: analyze limpo; testes verdes (incl. os de estado de erro/vazio e o novo esqueleto).

---

### Task 3: Catálogo `/design` + doc 15 §3

**Files:**
- Modify: `lib/features/design_system/ui/design_system_screen.dart`
- Modify: `docs/15-design-system.md` (§3)

- [ ] **Step 1: Catálogo** — em `design_system_screen.dart`, adicionar o import `package:.../core/widgets/app_esqueleto.dart` e, após a seção "Chips e estados" (antes do fecho do `Column`), acrescentar:

```dart
            const _Titulo('Esqueleto'),
            const AppCard(child: AppEsqueleto(linhas: 3)),
```

- [ ] **Step 2: Doc 15 §3** — trocar a linha do `AppEsqueleto`

De:

```
| `AppEsqueleto` | Placeholder estático de carregamento das listas (Fase 14) |
```

Para:

```
| `AppEsqueleto` | Placeholder estático de carregamento (blocos da cor de superfície, sem animação, sem pacote novo) — painel, itens da lista e membros (F14-T09) |
```

- [ ] **Step 3: Rodar e ver passar**

Run: `flutter analyze` e `flutter test test/features/design_system/ 2>$null; flutter test`
Expected: analyze limpo; suíte inteira verde (o catálogo é `kDebugMode`, sem teste dedicado por padrão).

---

### Task 4: Marcar F14-T09 e validação final

**Files:**
- Modify: `docs/14-tarefas.md` (F14-T09 + progresso)

- [ ] **Step 1: Doc 14** — marcar F14-T09

```markdown
- [x] **F14-T09** — Skeletons + fechamento
  Dep: F14-T01…T08 · Docs: [15 §3](15-design-system.md), [07 §1](07-qualidade-ci.md)
  CP: `AppEsqueleto` (estático, sem dependência nova) no painel/itens/membros; `format`/`analyze`/`test` verdes; docs sincronizados; APK aos testadores via F5-T05b (opcional).
  *(`AppEsqueleto` estático com `linhas`/`altura`, cor `surfaceContainerHighest`, `Semantics` de carregamento; usado no painel de listas (4), itens da lista (5) e membros (4), substituindo o spinner só nessas listas; catálogo `/design` e doc 15 §3 atualizados; APK opcional não executado)*
```

Na tabela de progresso:

```markdown
| F14 Acessibilidade & UX | 10 | 10 |
```

E o total:

```markdown
| **Total** | **97** | **95** |
```

- [ ] **Step 2: Validação final**

Run: `dart format --set-exit-if-changed .`
Run: `flutter analyze`
Run: `flutter test`
Expected: format sem alterações; analyze "No issues found!"; toda a suíte verde.

- [ ] **Step 3: Commit (somente com confirmação do usuário)**

```bash
git add lib test docs
git commit -m "F14-T09: AppEsqueleto no painel, itens e membros (RNF-06)"
```

> **Nota:** não commitar sem confirmação explícita. Se o usuário autorizar, decidir o agrupamento com ele.

## Self-Review

- **Spec coverage (§11):** placeholder estático sem animação/pacote (Task 1); painel/itens/membros (Task 2); spinner mantido onde o conteúdo é pequeno (nenhuma troca em botões/modal). Docs donos (Tasks 3/4). APK opcional declarado (não executado). ✔
- **Placeholders:** nenhum "TBD"; todos os passos têm código/teste reais e comandos executáveis.
- **Type consistency:** `AppEsqueleto({int linhas, double altura})` nasce na Task 1 e é consumido na Task 2 e no catálogo (Task 3) com os mesmos nomes; `AppStrings.carregando` já existe.
- **Riscos:** nenhum teste existente asserta `CircularProgressIndicator` nessas três telas (verificado por busca) — a troca não quebra a suíte; o catálogo é `kDebugMode` e não tem teste dedicado.
