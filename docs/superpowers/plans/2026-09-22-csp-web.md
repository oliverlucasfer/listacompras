# Fase 35 — CSP recomendada no Web (R-21): Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Documentar a política CSP recomendada (e COOP/COEP) no doc 06 §3.4.1, pronta para quando o Web for servido/publicado — sem aplicar CSP ao vivo (ADR-013).

**Architecture:** Mudança **só de documentação** no doc dono 06 + fechamento em 14/16. Nenhum arquivo de `web/`, nenhum código.

**Tech Stack:** Markdown (pt-BR).

**Spec:** `docs/superpowers/specs/2026-09-22-csp-web-design.md`

## Global Constraints

- Só `docs/06-mvp-entregas.md`, `docs/14-tarefas.md`, `docs/16-roadmap-pos-mvp.md`. Nada de `web/`, `lib/`, `supabase/`, `ci.yml`, `docs/01/02/03`.
- **Não aplicar** CSP ao vivo (sem `<meta>`, sem `web/_headers`) — só documentar (R-21 = recomendação; ADR-013 = Web local-only).
- Uma tarefa = um commit, `F35-Tnn: <resumo>` em pt-BR.
- Gate por tarefa: `dart format . && flutter analyze && flutter test` verdes (sem código novo; baseline 589).
- Push/merge só com autorização do dono.

---

### Task 1: doc 06 §3.4.1 — CSP recomendada

**Files:**
- Modify: `docs/06-mvp-entregas.md` (§3.4.1)

- [ ] **Step 1: Substituir a frase genérica pela CSP recomendada**

No §3.4.1, substituir o bullet final — `* Endurecimento futuro (ex.: CSP) só volta a fazer sentido se a publicação for retomada.` — por uma subseção com a política pronta (mantendo o estilo do arquivo):

```markdown
* **CSP recomendada (R-21)** — aplicar **quando o Web for servido por um static server ou a publicação for retomada** (não se aplica ao `flutter run -d chrome`, que já funciona sem configuração extra). Prefira **header HTTP**: em `<meta>`, `frame-ancestors`, `report-uri`/`report-to` e `sandbox` são ignorados.

  ```
  Content-Security-Policy:
    default-src 'self';
    script-src 'self' 'wasm-unsafe-eval';
    style-src 'self' 'unsafe-inline';
    img-src 'self' data: blob:;
    font-src 'self' data:;
    connect-src 'self' https://SEU-PROJETO.supabase.co wss://SEU-PROJETO.supabase.co;
    worker-src 'self' blob:;
    object-src 'none';
    base-uri 'self';
    form-action 'self';
    frame-ancestors 'none'
  ```

  Cabeçalhos de isolamento (SharedArrayBuffer/OPFS do Drift WASM):

  ```
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  ```

  Notas: `script-src 'wasm-unsafe-eval'` cobre o CanvasKit/skwasm; `style-src 'unsafe-inline'` é exigido pelos estilos inline do Flutter; `worker-src 'self' blob:` cobre o `drift_worker.js` (`sqlite3.wasm`/`drift_worker.js` vêm do próprio `build/web`); `connect-src` precisa das origens **https e wss** do projeto Supabase (ajuste `SEU-PROJETO`); com COEP `require-corp`, recursos cross-origin exigem CORP — os assets do Flutter são locais, então é compatível.
```

- [ ] **Step 2: Gate e commit**

Run: `dart format . && flutter analyze && flutter test`
Expected: verde (589).

```bash
git add docs/06-mvp-entregas.md
git commit -m "F35-T01: CSP recomendada no doc 06 (R-21)"
```

---

### Task 2: Fechamento — tarefas (14) e roadmap (16)

**Files:**
- Modify: `docs/14-tarefas.md`
- Modify: `docs/16-roadmap-pos-mvp.md`

- [ ] **Step 1: doc 14 — Fase 35**
  - Bloco `## Fase 35 — CSP recomendada no Web (R-21)` com `Spec: docs/superpowers/specs/2026-09-22-csp-web-design.md · Referência: R-21 · ADR: ADR-013 · Docs donos: 06`, e F35-T01…T02 `[x]` com `Dep:`/`Docs:`/`CP:` no estilo da F34.
  - Tabela "Progresso por fase": `| F35 CSP no Web | 2 | 2 |` e total `| **Total** | **179** | **177** |`.

- [ ] **Step 2: doc 16 — C4 concluído**
  - Linha C4 → `concluído (F35-T01…T02) — CSP recomendada (e COOP/COEP) documentada no 06 §3.4.1; aplicável quando o Web for servido/publicado (ADR-013)`, no estilo das linhas C2/C3.

- [ ] **Step 3: Gate e commit**

Run: `dart format . && flutter analyze && flutter test`
Expected: verde.

```bash
git add docs/14-tarefas.md docs/16-roadmap-pos-mvp.md
git commit -m "F35-T02: fechamento da Fase 35 (R-21)"
```

---

## Self-review (preenchido pelo autor do plano)

- **Cobertura do spec:** §3 política → T01; §4 docs donos → T01/T02; §2 "fora" (sem `<meta>`/`_headers`) → constraint global.
- **Placeholders:** nenhum "TBD"; bloco de CSP completo.
- **Consistência:** progresso 177→179 / 175→177; T01 só doc 06, T02 só 14/16 (sem arquivos compartilhados).
- **Sem código:** nenhum `web/`, `lib/` ou migration tocado.
